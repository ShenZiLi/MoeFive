# MoeFive 项目协作约定

## 项目基线

- 使用 Godot 4.7.2、GDScript 和 Compatibility 渲染器；不要迁移到 C# 或 Forward+。
- 目标平台包含 Windows、macOS、Linux、Android、iOS 和 Web。美术效果以 Compatibility 渲染器为准。
- 项目视觉为中文萌宠风格。用户提供的原型图是布局和美术对齐依据；不要未经请求替换、裁切或重新设计已有视觉方向。
- 复用现有 `scripts/`、`scenes/`、`assets/` 结构和对局状态接口。棋盘数据以 `Board` 为准，UI 根据状态对象更新，不在界面层另算胜负或落子数。

## 修改与验证

- 开始修改前先查看 `git status` 和相关实现，保留工作区已有改动，不覆盖或混入无关变更。
- 按用户请求完成改动后，运行与改动直接相关的 Godot 导入、场景检查或既有测试；不要为了无关覆盖率扩展测试范围。
- 对 UI 和美术改动，在目标窗口尺寸实际运行并检查截图；桌面与竖屏布局都受影响时，两者都要查看。
- 新增字体、图片、音频等资源时，确认文件格式、透明通道（如适用）、导入设置和授权记录。

## 本地 Git 提交

- 每个用户请求对应的完整、可审查改动集完成并验证后，都要创建一个本地 Git commit，再向用户报告。
- 提交前检查 diff 和工作区状态；只暂存本次请求涉及的文件或改动块。已有的用户改动、其他任务文件及生成缓存不得顺手提交。
- 若同一文件同时包含本次改动和已有未提交改动，使用分块暂存，只提交本次改动；提交后确认其余改动仍留在工作区。
- 提交信息简洁说明变更，可沿用仓库现有 Conventional Commit 风格。
- 只提交到当前本地分支；除非用户明确要求，不推送远端、不改写已有提交，也不使用 `--amend`。
